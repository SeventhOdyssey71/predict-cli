# predict-cli does NOT read any private key. Signing uses the local
# `sui` CLI's keystore (~/.sui/sui_config/sui.keystore) via shell-out.
# These two variables are optional overrides only; defaults work fine.

# RPC_URL=https://fullnode.testnet.sui.io
# PREDICT_SERVER=https://predict-server.testnet.mystenlabs.com
